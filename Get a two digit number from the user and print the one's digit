#include <stdio.h>

int main() {
    int num,digit;
    printf("Enter the number:");
    scanf("%d",&num);
// while(num>0)
// {
    digit=num%10;
    num=num/10;
    printf("%d One's digit is",digit);
//}
    return 0;
}
